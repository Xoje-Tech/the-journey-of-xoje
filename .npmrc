auto-install-peers=true
enable-pre-post-scripts=true
# pnpm 11: enable native build scripts for esbuild (Astro bundler) and
# sharp (Astro image pipeline). Both are required for `astro build`.
onlyBuiltDependencies[]=esbuild
onlyBuiltDependencies[]=sharp
